{
  "name": "weather-api",
  "version": "1.0.0",
  "description": "Creating API for Weather Forecast",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kanishk",
  "license": "ISC",
  "dependencies": {
    
    "requests": "^0.3.0"
  }
}
